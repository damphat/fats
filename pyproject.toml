[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fats"
version = "0.3.0"
description = "My personal python utillities"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []